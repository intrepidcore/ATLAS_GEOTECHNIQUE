# Build artifacts
# Local / heavy rasters (never commit)
ressource/DSM/rasters_COP30/
*.tif
target/
services/api-geo/target/
ui/node_modules/
ui/dist/

# SQLx offline data (on garde pour offline mode)
# .sqlx/

# Database data - ON GARDE pour pouvoir restore
# data/db/

# ===========================================
# FICHIERS D'ENVIRONNEMENT (ne pas versionner)
# ===========================================
.env
.env.local
.env.*
.env.*.local

# Garder uniquement les modèles (.env.example)
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.temp
data/db/**
!data/db/backups/
!data/db/backups/atlas_desktop_seed.dump
!data/db/backups/atlas_desktop_seed.dump.json

# Archived seed versions are local artifacts (not tracked)
data/db/backups/versions/*.dump
data/db/backups/versions/*.dump.json

# ===========================================
# ARTEFACTS LOURDS / LOCAUX (ne pas versionner)
# ===========================================
exports/*.pdf
exports/*.xlsx
exports/*.csv
exports/*.json
data/thematic_maille_matrix_fixed.xlsx
data/tiles/*.mbtiles
backup/
db/dumps/

# QGIS (projets/GeoPackages locaux)
QGIS/*.gpkg
QGIS/*.gpkg-*
QGIS/*.qgz

# Analyses locales (brutes)
atlas_geotechnique_2025-12-29/donnees_analyse/donnees_brutes/

# Exports Atlas locaux (images/logs) - ne jamais versionner
atlas_geotechnique*/
atlas_geotechnique_*/

# Docs images (captures/exports)
docs/image/

# Local Netlify folder
.netlify

# Fichiers de données (non-suivis dans git)
data/**/*.xlsx
data/**/*.csv

# Fichiers temporaires Atlas
token.txt
temp_*.ps1
temp_*.sql
prec_*.txt
psql_audit*.ps1
smoke_tests*.ps1
start_atlas.ps1

# Binaires de backup
api-geo-backup.exe
atlas-pro-backup.exe
