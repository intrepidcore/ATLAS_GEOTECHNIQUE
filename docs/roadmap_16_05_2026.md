══════════════════════════════════════════════════════════════════
# ATLAS GÉOTECHNIQUE — ROADMAP CORRECTIFS v3.5
### Intrepid Core Engineering Standards
**Session de continuation — 16/05/2026**
══════════════════════════════════════════════════════════════════

### VISION INTREPID CORE :
> "Innover localement, impacter globalement."

Chaque ligne de code produite doit être :
- **Traçable** (auditée, versionnée, reproductible)
- **Fiable** (testée, validée, documentée)
- **Durable** (maintenable sans l'auteur)
- Platform-grade, pas prototype-grade.

### ÉTAT ACQUIS (NE PAS RÉEXÉCUTER) :
- ✅ EDA N=29407 (fix `ai_stats.rs fetch_optional`)
- ✅ Clamp WL≥10, WP≥5, IP≤60, EG≤20, VBS≤15
- ✅ ML Registry `rmse_cv=2.773`, `r2_cv=-0.019`
- ✅ variogram-compare SVG=11542 chars
- ✅ coverage-map `coverage_pct=100.0`
- ✅ vbs_ked_h3 thematic `count=29407` (fix `routes.rs`)
- ✅ Dump DB v1.2.1 créé (757 MB)

### INCERTITUDES À RÉSOUDRE EN PRIORITÉ :
- ❓ Doublons `ai_variograms` (pas de SQL direct confirmant 0)
- ❓ `_avg` derived 100% (token expiré, pas de SQL direct)
- ❓ Manifest v1.2.1 incomplet (champs contrat v2 manquants)
- ❌ LOO RMSE 0.0000 en UI (bug B4 non résolu)
- ❌ Validation UI non faite (déléguée à Serge)

### RÈGLES D'EXÉCUTION INTREPID CORE :
1. **Bash > Glob** pour l'accès aux fichiers Windows
2. **PowerShell** : jamais `head`/`grep`/`tail`/`uniq` → `Select-String`
3. Commandes longues → `Start-Process PowerShell` détaché
4. **3 niveaux de test** : L1=unitaire, L2=intégration, L3=E2E
5. Chaque fix = preuve `curl` ou SQL directe avant de clôturer
6. Dump DB final OBLIGATOIRE en dernière étape
7. Ne jamais déclarer une tâche complète sans preuve mesurable

### CONTEXTE :
- **Repo** : `C:\PROJET_ATLAS_MASTER\atlas_reclone`
- **DB**   : `postgresql://atlas:atlas@127.0.0.1:5433/atlas_clean`
- **API**  : `http://127.0.0.1:8000`
- **PSQL** : `"C:\Program Files\QGIS 3.44.8\bin\psql.exe"`
- **UI**   : `http://localhost:1420`

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 0 — ÉTAT DES LIEUX (15 min MAX)
*Mesurer avant de corriger — principe Intrepid Core*
══════════════════════════════════════════════════════════════════

**0A — Token frais :**
```bash
curl -s -X POST http://127.0.0.1:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"admin@atlas.local\",\"password\":\"Atlas2024!\"}"
```
*Extraire `access_token` → `$TOKEN`*

**0B — Sanity DB en une seule requête :**
```powershell
$env:PGPASSWORD = 'atlas'
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
SELECT
  (SELECT COUNT(*) FROM atlas.mailles) AS mailles,
  (SELECT COUNT(DISTINCT parameter_id)
   FROM atlas.ai_interpolation_values
   WHERE COALESCE(is_superseded,false)=false) AS params_actifs,
  (SELECT COUNT(*) FROM atlas.ai_variograms) AS vario_total,
  (SELECT MAX(cnt) FROM (
     SELECT COUNT(*) as cnt FROM atlas.ai_variograms
     GROUP BY parameter_id
  ) t) AS max_doublons_vario,
  (SELECT COUNT(*) FROM atlas.ai_interpolation_values
   WHERE parameter_id = 'vbs_avg'
     AND COALESCE(is_superseded,false)=false) AS vbs_avg_n,
  (SELECT COUNT(*) FROM atlas.ai_interpolation_values
   WHERE parameter_id = 'eg_avg'
     AND COALESCE(is_superseded,false)=false) AS eg_avg_n,
  (SELECT COUNT(*) FROM atlas.ai_interpolation_values
   WHERE parameter_id = 'ip_avg'
     AND COALESCE(is_superseded,false)=false) AS ip_avg_n,
  (SELECT COUNT(*) FROM atlas.ai_interpolation_values
   WHERE parameter_id = 'passant_2mm_avg'
     AND COALESCE(is_superseded,false)=false) AS p2mm_avg_n,
  (SELECT COUNT(*) FROM atlas.ai_interpolation_values
   WHERE parameter_id = 'passant_80um_avg'
     AND COALESCE(is_superseded,false)=false) AS p80um_avg_n;"
```

NOTER chaque valeur. Les résultats attendus :
- `mailles=29407`
- `params_actifs≥32`
- `max_doublons_vario=1` (aucun doublon)
- `vbs_avg_n=29407`
- `eg_avg_n=29407`
- `ip_avg_n=29407`
- `p2mm_avg_n=29407`
- `p80um_avg_n=29407`

> *Si `max_doublons_vario > 1` → étape 1A obligatoire*
> *Si `X_avg_n ≠ 29407` → étape 1B obligatoire*

**0C — Vérifier l'état de l'API :**
```bash
curl -s http://127.0.0.1:8000/healthz
```
*ATTENDU : ok*

**0D — Vérifier le manifest actuel :**
```powershell
$j = Get-Content "data\db\backups\atlas_desktop_seed.dump.json" | ConvertFrom-Json
Write-Output "seed_version: $($j.identity.seed_version)"
Write-Output "invariants présents: $(($j.invariants | Measure-Object).Count)"
```
*ATTENDU : `seed_version=1.2.1`, `invariants≥5`*
> *Si invariants absent → étape 3 obligatoire*

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 1A — NETTOYAGE DOUBLONS `ai_variograms`
*(Exécuter seulement si `max_doublons_vario > 1` en étape 0B)*
══════════════════════════════════════════════════════════════════

**1A-1 — Voir exactement ce qui sera supprimé (DRY RUN) :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
SELECT parameter_id, COUNT(*) as n_doublons,
  MIN(created_at)::date as premier,
  MAX(created_at)::date as dernier
FROM atlas.ai_variograms
GROUP BY parameter_id
HAVING COUNT(*) > 1
ORDER BY n_doublons DESC;"
```

**1A-2 — Supprimer en gardant le plus récent avec nugget non nul :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
BEGIN;

DELETE FROM atlas.ai_variograms
WHERE id NOT IN (
  SELECT DISTINCT ON (parameter_id) id
  FROM atlas.ai_variograms
  ORDER BY parameter_id,
           (nugget IS NOT NULL) DESC,
           (loo_rmse IS NOT NULL) DESC,
           created_at DESC
);

-- Vérifier le résultat avant COMMIT
SELECT parameter_id, COUNT(*) as n
FROM atlas.ai_variograms
GROUP BY parameter_id
HAVING COUNT(*) > 1;

COMMIT;"
```
*ATTENDU : 0 lignes retournées par le SELECT intermédiaire*

**1A-3 — TEST L1 : Confirmer 0 doublon :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
SELECT COALESCE(MAX(cnt),0) as max_doublons
FROM (
  SELECT COUNT(*) as cnt FROM atlas.ai_variograms
  GROUP BY parameter_id HAVING COUNT(*) > 1
) t;"
```
*ATTENDU : `max_doublons=0`*

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 1B — COMPLÉTER LES `_avg` À 100%
*(Exécuter seulement si `X_avg_n ≠ 29407` en étape 0B)*
══════════════════════════════════════════════════════════════════

**CONTEXTE MÉTIER :**
`vbs_avg`, `ip_avg`, `eg_avg`, `passant_2mm_avg`, `passant_80um_avg` sont des cartes de synthèse nationale (moyenne H1+H2+H3). Sans couverture 100%, elles ne peuvent pas figurer dans le mémoire comme cartes nationales. Elles héritent la couverture 100% des KED. Méthode correcte = `derived_avg_from_ked` (pas kriging ordinaire).

**1B-1 — Marquer les anciens _avg comme superseded :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
UPDATE atlas.ai_interpolation_values
SET is_superseded = true
WHERE parameter_id IN (
  'vbs_avg','ip_avg','eg_avg',
  'passant_2mm_avg','passant_80um_avg'
)
  AND method != 'derived_avg_from_ked';

-- Confirmer
SELECT parameter_id, method, is_superseded, COUNT(*) as n
FROM atlas.ai_interpolation_values
WHERE parameter_id IN (
  'vbs_avg','ip_avg','eg_avg',
  'passant_2mm_avg','passant_80um_avg'
)
GROUP BY parameter_id, method, is_superseded
ORDER BY parameter_id, is_superseded;"
```

**1B-2 — Lire le script de dérivation existant :**
```bash
cat "C:\PROJET_ATLAS_MASTER\atlas_reclone\scripts\derive_wl_wp_avg.py"
```
*Identifier la liste `PARAMS_TO_DERIVE` et la logique SQL `INSERT`. Vérifier si les 5 nouveaux paramètres sont déjà dans la liste.*

**1B-3 — Si absents, les ajouter dans le script Python :**
Dans `PARAMS_TO_DERIVE`, ajouter :
```python
('vbs',         'vbs_ked_h1',         'vbs_ked_h2',         'vbs_ked_h3',         'vbs_avg'),
('ip',          'ip_ked_h1',          'ip_ked_h2',          'ip_ked_h3',          'ip_avg'),
('eg',          'eg_ked_h1',          'eg_ked_h2',          'eg_ked_h3',          'eg_avg'),
('passant_2mm', 'passant_2mm_ked_h1', 'passant_2mm_ked_h2', 'passant_2mm_ked_h3', 'passant_2mm_avg'),
('passant_80um','passant_80um_ked_h1','passant_80um_ked_h2','passant_80um_ked_h3','passant_80um_avg'),
```

**1B-4 — Lancer le script :**
```powershell
$env:DATABASE_URL = 'postgresql://atlas:atlas@127.0.0.1:5433/atlas_clean'
python scripts\derive_wl_wp_avg.py
```

**1B-5 — TEST L1 : Vérifier la couverture SQL directe :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
SELECT parameter_id, method,
  COUNT(DISTINCT maille_id) as n,
  ROUND(COUNT(DISTINCT maille_id)::numeric * 100.0 / 29407, 2) as pct
FROM atlas.ai_interpolation_values
WHERE parameter_id IN (
  'vbs_avg','ip_avg','eg_avg',
  'passant_2mm_avg','passant_80um_avg',
  'wl_avg','wp_avg'
)
  AND COALESCE(is_superseded,false)=false
GROUP BY parameter_id, method
ORDER BY parameter_id;"
```
*ATTENDU : Tous à 29407 (100%), method=derived_avg_from_ked*

**1B-6 — TEST L2 : Via API :**
```bash
curl "http://127.0.0.1:8000/api/stats/descriptive?parameter=vbs_avg" \
  -H "Authorization: Bearer $TOKEN"
```
*ATTENDU : `n=29407`, `source=interpolated_values`*

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 2 — FIX B4 : LOO RMSE = 0.0000 dans Expert Scientifique
*BUG NON RÉSOLU — priorité haute pour le mémoire*
══════════════════════════════════════════════════════════════════

**DIAGNOSTIC ÉTABLI :**
L'API `/ai/variograms/summary` retourne bien `loo_rmse=1.667` pour `eg_ked_h1`. Le frontend affiche `0.0000`.
Cause probable : l'onglet `VariogrammesTab` de `ExpertScientificDbTab.tsx` appelle `tablesApi.getData('atlas','ai_variograms')` qui lit la table BRUTE avec `COALESCE(loo_rmse, 0)` quelque part, au lieu de `/ai/variograms/summary` qui retourne les valeurs réelles.

**2A — Localiser la source de données dans VariogrammesTab :**
```powershell
Select-String `
  -Path "C:\PROJET_ATLAS_MASTER\atlas_reclone\ui\src\components\ExpertScientificDbTab.tsx" `
  -Pattern "tablesApi|variograms/summary|ai_variograms|getData" |
  Select-Object LineNumber, Line | Format-Table -AutoSize
```
Pour chaque occurrence, noter :
- Quelle ligne appelle `tablesApi.getData` sur `ai_variograms` ?
- Quelle ligne appelle `fetch('/ai/variograms/summary')` ?

**2B — Identifier le problème exact :**
Si le VariogrammesTab utilise `tablesApi.getData('atlas','ai_variograms')`
→ il lit la table brute sans l'endpoint summary
→ le champ `loo_rmse` peut être null dans la table brute pour certaines lignes
→ `COALESCE` ou `Number(null)` → `0` → affichage `0.0000`

Si le VariogrammesTab utilise bien `/ai/variograms/summary`
→ le bug est dans le rendering (`Number(null).toFixed` vs `fmtDash`)

**2C — Corriger selon le cas :**

**CAS A — `tablesApi.getData` sur `ai_variograms` (le plus probable) :**
Dans le `useEffect` du `VariogrammesTab` qui charge les variogrammes pour la section 'multi-domaines', remplacer l'appel tablesApi par un fetch direct vers `/ai/variograms/summary` :

```typescript
// AVANT (lit la table brute, loo_rmse peut être null ou 0 raw)
const data = await tablesApi.getData('atlas', 'ai_variograms', 100, 0)
setVariograms(data as Row[])

// APRÈS (utilise l'endpoint summary avec valeurs réelles)
const token = localStorage.getItem('atlas_token') ?? ''
const res = await fetch(
  `${import.meta.env.VITE_API_URL ?? 'http://127.0.0.1:8000'}/ai/variograms/summary`,
  { headers: { Authorization: `Bearer ${token}` } }
)
if (!res.ok) throw new Error(`variograms/summary: ${res.status}`)
const json = await res.json()
// L'endpoint retourne {count, items, success}
setVariograms((json.items ?? []) as Row[])
```

**CAS B — La source est déjà `/ai/variograms/summary` :**
Vérifier le rendering de la colonne `loo_rmse` dans la DataTable. S'assurer que la fonction `fmtDash` ou le render custom traite bien null (pas `Number(null)` qui donne 0) :
```typescript
render: (_v, row) => {
  const val = row.loo_rmse
  if (val === null || val === undefined) {
    return <span style={{ color: '#94A3B8' }}>—</span>
  }
  const n = Number(val)
  if (!Number.isFinite(n) || n === 0) {
    return <span style={{ color: '#94A3B8' }}>—</span>
  }
  return <Databar value={n} maxVal={20} />
}
```

**2D — TEST L1 : Vérifier que la source API retourne les vraies valeurs :**
```bash
curl "http://127.0.0.1:8000/ai/variograms/summary" \
  -H "Authorization: Bearer $TOKEN"
```
*Filtrer `eg_ked_h1` et noter `loo_rmse`.*
*ATTENDU : `loo_rmse ≈ 1.667` (pas 0 ou null)*

**2E — npm run build :**
```powershell
cd C:\PROJET_ATLAS_MASTER\atlas_reclone\ui
npm run build 2>&1 | Select-Object -Last 3
```
*ATTENDU : `✓ built in Xs`*

**2F — npm run test:unit :**
```powershell
npm run test:unit 2>&1 | Select-Object -Last 3
```
*ATTENDU : `31 passed (31)`*

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 3 — MANIFEST SEED DUMP v1.2.1 — Mise en conformité contrat v2
*Standard Intrepid Core : un dump sans manifest complet est invalide*
══════════════════════════════════════════════════════════════════

**CONTEXTE :**
Le manifest produit est simplifié. Le contrat Seed Dump v2 définit des champs obligatoires (`INV-001` à `INV-006`) qui permettent la vérification automatique du dump lors du restore MSI. Sans ces invariants, le restore de l'installateur desktop ne peut pas valider l'intégrité de la base après installation.

**3A — Calculer les métriques actuelles pour le manifest :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
SELECT
  (SELECT COUNT(*) FROM atlas.mailles) AS n_mailles,
  (SELECT COUNT(*) FROM atlas.ai_interpolation_values
   WHERE COALESCE(is_superseded,false)=false) AS n_interp,
  (SELECT COUNT(*) FROM atlas.sondages
   WHERE deleted_at IS NULL) AS n_sondages,
  (SELECT COUNT(DISTINCT parameter_id)
   FROM atlas.ai_interpolation_values
   WHERE COALESCE(is_superseded,false)=false) AS n_params,
  (SELECT COUNT(*) FROM atlas.ai_variograms) AS n_vario,
  (SELECT COUNT(*) FROM atlas.users) AS n_users,
  (SELECT COUNT(*) FROM information_schema.tables
   WHERE table_schema = 'atlas') AS n_tables;"
```
*NOTER toutes les valeurs.*

**3B — Calculer le SHA256 du dump actuel :**
```powershell
$hash = (Get-FileHash `
  "data\db\backups\atlas_desktop_seed.dump" `
  -Algorithm SHA256).Hash
$size = (Get-Item `
  "data\db\backups\atlas_desktop_seed.dump").Length
Write-Output "SHA256: $hash"
Write-Output "Size: $size bytes"
```

**3C — Écrire le manifest complet conforme au contrat v2 :**
Créer `data\db\backups\atlas_desktop_seed.dump.json` avec :

```json
{
  "$schema": "https://atlas.intrepidcore.io/schemas/seed-manifest-v2.json",
  "schema_version": "2.0",

  "identity": {
    "seed_id": "atlas-seed-20260516-v1.2.1",
    "seed_version": "1.2.1",
    "environment": "production",
    "classification": "internal"
  },

  "source": {
    "created_at": "2026-05-16T14:14:43Z",
    "created_by": "serge.tabedjato",
    "git_commit": "session-20260516",
    "git_branch": "atlas_v2_clean",
    "db_name": "atlas_clean",
    "format": "pg_dump -Fc --no-owner --no-privileges"
  },

  "integrity": {
    "sha256": "[VALEUR DE 3B]",
    "size_bytes": [VALEUR DE 3B],
    "signed_by": "atlas-release-key-2026"
  },

  "compatibility": {
    "postgres_min_major": 16,
    "postgres_max_major": 17,
    "postgis_min_version": "3.4",
    "max_migration_applied": 148,
    "min_migration_applied": 1,
    "requires_extensions": ["postgis", "uuid-ossp", "pg_trgm"]
  },

  "contents": {
    "schemas": ["public", "atlas"],
    "tables_count": [N_TABLES DE 3A],
    "rows_estimate": {
      "atlas.mailles": 29407,
      "atlas.sondages": [N_SONDAGES DE 3A],
      "atlas.ai_interpolation_values": [N_INTERP DE 3A],
      "atlas.ai_variograms": [N_VARIO DE 3A],
      "atlas.users": 0
    },
    "contains_pii": false,
    "contains_user_data": false,
    "geographic_scope": "TGO"
  },

  "retention": {
    "expires_at": "2027-05-16T00:00:00Z",
    "superseded_by": null,
    "keep_versions": 3
  },

  "invariants": [
    {
      "id": "INV-001",
      "description": "Toutes les mailles V2 présentes",
      "severity": "critical",
      "query": "SELECT COUNT(*) FROM atlas.mailles",
      "expected_min": 29407,
      "expected_max": 29407
    },
    {
      "id": "INV-002",
      "description": "Table desktop_seed_state présente",
      "severity": "critical",
      "query": "SELECT to_regclass('atlas.desktop_seed_state') IS NOT NULL",
      "expected_value": true
    },
    {
      "id": "INV-003",
      "description": "PostGIS opérationnel",
      "severity": "critical",
      "query": "SELECT ST_IsValid(ST_GeomFromText('POINT(0 0)'))",
      "expected_value": true
    },
    {
      "id": "INV-004",
      "description": "KED complet — 21 paramètres à 29407 mailles",
      "severity": "critical",
      "query": "SELECT COUNT(*) FROM (SELECT parameter_id FROM atlas.ai_interpolation_values WHERE COALESCE(is_superseded,false)=false GROUP BY parameter_id HAVING COUNT(DISTINCT maille_id) = 29407 AND parameter_id LIKE '%_ked_%') t",
      "expected_min": 21
    },
    {
      "id": "INV-005",
      "description": "Aucun utilisateur dans le seed (sécurité)",
      "severity": "critical",
      "query": "SELECT COUNT(*) FROM atlas.users",
      "expected_max": 0
    },
    {
      "id": "INV-006",
      "description": "ML Registry opérationnel",
      "severity": "high",
      "query": "SELECT COUNT(*) FROM atlas.ai_model_registry WHERE status = 'active'",
      "expected_min": 1
    }
  ],

  "changelog": [
    "v1.2.1 (2026-05-16): Fix EDA N=1 (ai_stats.rs fetch_optional), clamp WL/WP/IP bornes physiques, _avg derived 100% (29407 mailles chacun), fix thematic H3 (routes.rs contournement vue lente), doublons ai_variograms nettoyés",
    "v1.2.0 (2026-04-30): Pipeline KED complet 21 paramètres × 3 horizons",
    "v1.0.0 (2026-03-13): Seed initial"
  ]
}
```

**3D — TEST L1 : Valider le manifest JSON :**
```powershell
$j = Get-Content "data\db\backups\atlas_desktop_seed.dump.json" | ConvertFrom-Json
Write-Output "Version: $($j.identity.seed_version)"
Write-Output "SHA256: $($j.integrity.sha256)"
Write-Output "Invariants: $(($j.invariants | Measure-Object).Count)"
Write-Output "Changelog: $(($j.changelog | Measure-Object).Count) entrées"
```
*ATTENDU : Version=1.2.1, Invariants=6, Changelog=3*

**3E — Vérifier chaque invariant manuellement :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
-- INV-001
SELECT COUNT(*) AS inv001_mailles FROM atlas.mailles;
-- INV-003
SELECT ST_IsValid(ST_GeomFromText('POINT(0 0)')) AS inv003_postgis;
-- INV-004
SELECT COUNT(*) AS inv004_ked_21 FROM (
  SELECT parameter_id FROM atlas.ai_interpolation_values
  WHERE COALESCE(is_superseded,false)=false
  GROUP BY parameter_id
  HAVING COUNT(DISTINCT maille_id)=29407
    AND parameter_id LIKE '%_ked_%'
) t;
-- INV-005
SELECT COUNT(*) AS inv005_users FROM atlas.users;
-- INV-006
SELECT COUNT(*) AS inv006_ml FROM atlas.ai_model_registry
WHERE status='active';"
```
*ATTENDU : inv001=29407, inv003=t, inv004≥21, inv005=0, inv006≥1*

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 4 — SMOKE TESTS COMPLETS (9 tests)
*Preuves mesurables — standard Intrepid Core*
══════════════════════════════════════════════════════════════════

Écrire et exécuter le script PowerShell suivant :

**4A — Créer le script de smoke test :**
Écrire `C:\PROJET_ATLAS_MASTER\atlas_reclone\smoke_tests_v35.ps1`

```powershell
# Atlas Smoke Tests v3.5
# Intrepid Core Engineering Standards
# Usage: powershell -File smoke_tests_v35.ps1

param([string]$ApiUrl = "http://127.0.0.1:8000")

# Récupérer un token frais
$loginResp = Invoke-RestMethod -Uri "$ApiUrl/api/auth/login" `
  -Method POST -ContentType "application/json" `
  -Body '{"email":"admin@atlas.local","password":"Atlas2024!"}'
$TOKEN = $loginResp.access_token
$H = @{Authorization = "Bearer $TOKEN"}

$pass = 0; $fail = 0
$results = @()

function Test-Endpoint {
  param($name, $result, $condition, $detail)
  if ($condition) {
    Write-Host "  ✅ PASS: $detail" -ForegroundColor Green
    $script:pass++
  } else {
    Write-Host "  ❌ FAIL: $detail" -ForegroundColor Red
    $script:fail++
  }
  $script:results += [PSCustomObject]@{
    Test=$name; Pass=$condition; Detail=$detail
  }
}

Write-Host "`n=== ATLAS SMOKE TESTS v3.5 ===" -ForegroundColor Cyan
Write-Host "API: $ApiUrl | $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"

# T1 — EDA N=29407 source=interpolated_values
Write-Host "`n[T1] EDA eg_ked_h1 N=29407"
try {
  $r = Invoke-RestMethod "$ApiUrl/api/stats/descriptive?parameter=eg_ked_h1" -Headers $H
  Test-Endpoint "T1_EDA" $r ($r.n -eq 29407 -and $r.source -eq "interpolated_values") `
    "n=$($r.n) source=$($r.source)"
} catch { Test-Endpoint "T1_EDA" $null $false "EXCEPTION: $_" }

# T2 — WL clamp min≥10
Write-Host "`n[T2] WL clamp min≥10"
try {
  $r = Invoke-RestMethod "$ApiUrl/api/stats/descriptive?parameter=wl_ked_h1" -Headers $H
  Test-Endpoint "T2_WL" $r ([double]$r.min -ge 10.0) "min=$($r.min)"
} catch { Test-Endpoint "T2_WL" $null $false "EXCEPTION: $_" }

# T3 — IP clamp max≤60
Write-Host "`n[T3] IP clamp max≤60"
try {
  $r = Invoke-RestMethod "$ApiUrl/api/stats/descriptive?parameter=ip_ked_h2" -Headers $H
  Test-Endpoint "T3_IP" $r ([double]$r.max -le 60.0) "max=$($r.max)"
} catch { Test-Endpoint "T3_IP" $null $false "EXCEPTION: $_" }

# T4 — ML Registry rmse_cv et r2_cv
Write-Host "`n[T4] ML Registry"
try {
  $r = Invoke-RestMethod "$ApiUrl/api/stats/ml-registry" -Headers $H
  $ok = $r.count -ge 1 -and $r.items[0].rmse_cv -gt 0
  Test-Endpoint "T4_ML" $r $ok "count=$($r.count) rmse=$($r.items[0].rmse_cv)"
} catch { Test-Endpoint "T4_ML" $null $false "EXCEPTION: $_" }

# T5 — LOO RMSE eg_ked_h1≈1.667 via summary
Write-Host "`n[T5] LOO RMSE eg_ked_h1 via summary"
try {
  $r = Invoke-RestMethod "$ApiUrl/ai/variograms/summary" -Headers $H
  $eg = $r.items | Where-Object { $_.parameter_id -eq "eg_ked_h1" }
  $ok = $null -ne $eg -and [double]$eg.loo_rmse -gt 1.0
  Test-Endpoint "T5_LOO" $eg $ok "eg_ked_h1 loo_rmse=$($eg.loo_rmse)"
} catch { Test-Endpoint "T5_LOO" $null $false "EXCEPTION: $_" }

# T6 — vbs_ked_h3 thematic count=29407
Write-Host "`n[T6] vbs_ked_h3 thematic data"
try {
  $r = Invoke-RestMethod "$ApiUrl/api/thematic/data?parameter=vbs_ked_h3" -Headers $H
  $ok = $r.statistics.count -eq 29407
  Test-Endpoint "T6_H3" $r $ok "count=$($r.statistics.count) mean=$($r.statistics.mean)"
} catch { Test-Endpoint "T6_H3" $null $false "EXCEPTION: $_" }

# T7 — variogram-compare SVG>5000 chars
Write-Host "`n[T7] variogram-compare eg_ked"
try {
  $body = '{"parameter_base":"eg_ked","horizons":["h1","h2","h3"]}'
  $r = Invoke-RestMethod "$ApiUrl/ai/plots/variogram-compare" `
    -Method POST -ContentType "application/json" -Headers $H -Body $body
  $ok = $r.svg.Length -gt 5000
  Test-Endpoint "T7_VARIO" $r $ok "svg.length=$($r.svg.Length)"
} catch { Test-Endpoint "T7_VARIO" $null $false "EXCEPTION: $_" }

# T8 — coverage-map eg_ked_h1=100%
Write-Host "`n[T8] coverage-map eg_ked_h1"
try {
  $r = Invoke-RestMethod "$ApiUrl/api/stats/coverage-map?parameter=eg_ked_h1" -Headers $H
  $ok = [double]$r.coverage_pct -eq 100.0
  Test-Endpoint "T8_COV_MAP" $r $ok "coverage_pct=$($r.coverage_pct)"
} catch { Test-Endpoint "T8_COV_MAP" $null $false "EXCEPTION: $_" }

# T9 — DB sanity (mailles=29407, params≥32)
Write-Host "`n[T9] DB sanity via API"
try {
  $r = Invoke-RestMethod "$ApiUrl/api/stats/coverage" -Headers $H
  $params = ($r | Measure-Object).Count
  $ok = $params -ge 32
  Test-Endpoint "T9_DB" $r $ok "params_distincts=$params"
} catch { Test-Endpoint "T9_DB" $null $false "EXCEPTION: $_" }

# Rapport final
Write-Host "`n==============================="
Write-Host "SCORE : $pass/9" -ForegroundColor $(if($pass -eq 9){"Green"}else{"Yellow"})
Write-Host "==============================="

$results | Format-Table -AutoSize

# Sauvegarder le rapport
$report = @{
  timestamp = (Get-Date -Format "yyyy-MM-ddTHH:mm:ss")
  score = "$pass/9"
  tests = $results
} | ConvertTo-Json -Depth 3
$report | Out-File "smoke_report_$(Get-Date -Format 'yyyyMMdd_HHmmss').json"
Write-Host "Rapport sauvegardé."
```

**4B — Exécuter le script :**
```powershell
cd C:\PROJET_ATLAS_MASTER\atlas_reclone
powershell -File smoke_tests_v35.ps1
```
*ATTENDU : SCORE 9/9*
*Si < 9/9 → corriger les tests échoués avant de continuer.*

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 5 — DUMP DB FINAL v1.2.1 (après tout)
*Standard Intrepid Core : le dump final capture l'état validé*
══════════════════════════════════════════════════════════════════

**5A — Vérifier les invariants avant le dump final :**
```sql
& "C:\Program Files\QGIS 3.44.8\bin\psql.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean -c "
SELECT
  (SELECT COUNT(*) FROM atlas.mailles) = 29407 AS inv001_ok,
  (SELECT COUNT(*) FROM atlas.users) = 0 AS inv005_ok,
  (SELECT COUNT(*) FROM atlas.ai_model_registry
   WHERE status='active') >= 1 AS inv006_ok,
  (SELECT MAX(cnt) FROM (
    SELECT COUNT(*) cnt FROM atlas.ai_variograms
    GROUP BY parameter_id
  ) t) = 1 AS doublons_ok;"
```
*ATTENDU : inv001_ok=t, inv005_ok=t, inv006_ok=t, doublons_ok=t*
*Si NON → corriger avant de continuer.*

**5B — Archiver le dump précédent :**
```powershell
$verDir = "data\db\backups\versions"
New-Item -ItemType Directory -Force -Path $verDir | Out-Null
Copy-Item "data\db\backups\atlas_desktop_seed.dump" `
  "$verDir\atlas_desktop_seed_v1.2.1_pre_final.dump" -Force
Write-Output "Archive créée ✅"
```

**5C — Créer le dump final dans un nouveau process :**
Écrire d'abord `C:\PROJET_ATLAS_MASTER\atlas_reclone\do_dump.ps1` :
```powershell
$env:PGPASSWORD = 'atlas'
Write-Output "Démarrage dump $(Get-Date)..."
& "C:\Program Files\PostgreSQL\17\bin\pg_dump.exe" `
  -U atlas -h 127.0.0.1 -p 5433 -d atlas_clean `
  -Fc --no-owner --no-privileges -n atlas `
  -f "C:\PROJET_ATLAS_MASTER\atlas_reclone\data\db\backups\atlas_desktop_seed.dump"
Write-Output "Dump terminé $(Get-Date)"
$f = Get-Item "C:\PROJET_ATLAS_MASTER\atlas_reclone\data\db\backups\atlas_desktop_seed.dump"
Write-Output "Taille: $([math]::Round($f.Length/1MB,1)) MB"
$hash = (Get-FileHash $f.FullName -Algorithm SHA256).Hash
Write-Output "SHA256: $hash"
```
Puis lancer dans un process séparé :
```powershell
Start-Process powershell -ArgumentList "-File","do_dump.ps1" -Wait
```

**5D — Mettre à jour le SHA256 dans le manifest :**
```powershell
$hash = (Get-FileHash `
  "data\db\backups\atlas_desktop_seed.dump" `
  -Algorithm SHA256).Hash
$size = (Get-Item `
  "data\db\backups\atlas_desktop_seed.dump").Length
```
Ouvrir `data\db\backups\atlas_desktop_seed.dump.json`
Mettre à jour :
  `integrity.sha256` → `$hash`
  `integrity.size_bytes` → `$size`
  `source.created_at` → timestamp actuel ISO

**5E — TEST L1 final : vérifier cohérence manifest vs fichier :**
```powershell
$j = Get-Content "data\db\backups\atlas_desktop_seed.dump.json" | ConvertFrom-Json
$actualHash = (Get-FileHash `
  "data\db\backups\atlas_desktop_seed.dump" -Algorithm SHA256).Hash
if ($j.integrity.sha256 -eq $actualHash) {
  Write-Output "✅ SHA256 cohérent"
} else {
  Write-Output "❌ SHA256 incohérent — mettre à jour le manifest"
}
```

<br>

══════════════════════════════════════════════════════════════════
## ÉTAPE 6 — NETTOYAGE (standard Intrepid Core)
*Un repo propre est un repo maintenable*
══════════════════════════════════════════════════════════════════

**6A — Supprimer les fichiers temporaires de session :**
```powershell
Get-ChildItem "C:\PROJET_ATLAS_MASTER\atlas_reclone" `
  -Filter "temp_*.ps1" | Remove-Item -Force
Get-ChildItem "C:\PROJET_ATLAS_MASTER\atlas_reclone" `
  -Filter "temp_*.sql" | Remove-Item -Force
Get-ChildItem "C:\PROJET_ATLAS_MASTER\atlas_reclone" `
  -Filter "smoke_report_*.json" |
  Sort-Object LastWriteTime -Descending |
  Select-Object -Skip 1 |
  Remove-Item -Force
Remove-Item "do_dump.ps1" -ErrorAction SilentlyContinue
Remove-Item "api-geo-backup.exe" -ErrorAction SilentlyContinue
Write-Output "✅ Nettoyage terminé"
```

**6B — Vérifier qu'aucun fichier sensible n'est exposé :**
```powershell
Get-ChildItem "C:\PROJET_ATLAS_MASTER\atlas_reclone" -Filter ".env*" |
  Where-Object { $_.Name -ne ".env" } |
  ForEach-Object {
    Write-Output "⚠️ Fichier .env suspect : $($_.Name)"
  }
```

<br>

══════════════════════════════════════════════════════════════════
## RAPPORT FINAL DE SESSION
══════════════════════════════════════════════════════════════════

Produire ce rapport en fin de session :

## RAPPORT ATLAS ROADMAP v3.5
## Date : [DATE] | Intrepid Core Engineering
## DB : atlas_clean | Dump : v1.2.1 final

### SMOKE TESTS 9/9
[Copier le tableau du script smoke_tests_v35.ps1]

### CORRECTIONS APPLIQUÉES
| Étape | Tâche | Statut | Preuve |
|-------|-------|--------|--------|
| 1A | Doublons `ai_variograms` | ✅/❌/N/A | max_doublons=? |
| 1B | `_avg` derived 100% | ✅/❌/N/A | vbs_avg_n=? |
| 2 | LOO RMSE B4 | ✅/❌ | source changée tablesApi→summary |
| 3 | Manifest v1.2.1 complet | ✅/❌ | invariants=6 |
| 4 | Smoke tests 9/9 | ✅/❌ | score=?/9 |
| 5 | Dump DB final | ✅/❌ | SHA256=? |
| 6 | Nettoyage | ✅/❌ | |

### DB STATE FINAL
- mailles : 29407
- params_actifs : ?
- vbs_avg : ? mailles (% couverture)
- doublons_vario : 0

### SHA256 DUMP v1.2.1 FINAL
[Valeur complète]

<br>

══════════════════════════════════════════════════════════════════
## CHECKLIST VALIDATION UI — À FAIRE PAR SERGE
*(L'agent n'intervient pas sur cette partie)*
══════════════════════════════════════════════════════════════════

OUVRIR : `http://localhost:1420/index.html`

**CARTE INTERACTIVE — VÉRIFIER :**

- [ ] **1.** Header : indicateur "● API" est-il VERT ? (rouge = bug B3 encore présent)
- [ ] **2.** Header : Mailles = 29 407 ? Sondages = 113 ?
- [ ] **3.** Panneau Cartes thématiques → Source "Interpolation (Kriging)" → Sélectionner `vbs_ked` → Horizon H3 (2.0m) → Appliquer. La carte change-t-elle par rapport à H1 ? Cliquer une maille → la valeur affichée est-elle différente de H1 ? (si identique ou si H3 impossible à sélectionner → bug non résolu)
- [ ] **4.** Même paramètre, changer Horizon H1 puis H2 puis H3. Les légendes (bornes de classes) changent-elles à chaque fois ? ATTENDU : 3 légendes différentes (valeurs h1≠h2≠h3)
- [ ] **5.** Activer Mode expert → cocher Pédologie → Appliquer. La choroplèthe KED reste-t-elle visible sous la pédologie ? (si masquée = bug B1 encore présent)
- [ ] **6.** Recharger la page (F5). Le toggle Mode expert est-il désactivé après rechargement ? (si activé = bug B2 encore présent)
- [ ] **7.** Cliquer le bouton "Lama" (zones d'étude). La carte zoome-t-elle ? Un panneau statistiques s'ouvre-t-il ?
- [ ] **8.** Panneau gauche "Indicateur d'argilosité" : VBS moy. ≈ 4.1 | % argileux ≈ 58% | IP moy. ≈ 21% ?
- [ ] **9.** Cliquer une maille colorée (zone Lama) dans la carte. Le popup affiche-t-il le code maille (TG-XXXX) + une valeur VBS ?

OUVRIR : `http://localhost:1420/db-manager.html`

**DB MANAGER — ONGLET EXPERT SCIENTIFIQUE — VÉRIFIER :**

- [ ] **10.** Onglet "EDA / Statistiques descriptives" : Sélectionner `eg_ked_h1` → N = 29 407 ? (si N=1 → bug EDA encore présent) Source = "interpolated_values" ? Histogramme visible avec des barres colorées ?
- [ ] **11.** Onglet "Variogrammes" → sous-onglet "Multi-domaines" :
  - `eg_ked_h1` → LOO RMSE = 1.667 (pas 0.0000) ?
  - `vbs_ked_h1` → LOO RMSE = 3.060 (pas 0.0000) ?
  - `ip_ked_h1` → LOO RMSE = 10.531 (pas 0.0000) ?
  - La colonne Horizon affiche H1/H2/H3 (pas "—") ?
  - Pas de lignes dupliquées (`vbs_avg` une seule fois) ?
- [ ] **12.** Onglet "Validation" → LOO Validation : `eg_ked_h1` apparaît-il avec `loo_rmse ≈ 1.667` ?
- [ ] **13.** Onglet "Couverture des paramètres" :
  - `vbs_avg` → 100% ?
  - `ip_avg` → 100% ?
  - `eg_avg` → 100% ?
  - `passant_2mm_avg` → 100% ?
  - Toggle "Voir carte" visible → cliquer → mini-carte Leaflet visible ?
- [ ] **14.** Onglet "Registre ML" :
  - `rga_predictor` visible avec RMSE CV ≈ 2.77 ?
  - R² CV ≈ -0.019 affiché en rouge ?
  - Bandeau orange "R² négatifs détectés" présent ?
- [ ] **15.** Onglet "Données brutes" → `ai_variograms` : Pas de lignes avec `loo_rmse = 0` pour les paramètres KED ? `eg_ked_h1` → `loo_rmse ≈ 1.667` visible ?

**RÉSULTATS ATTENDUS :**
Tout coché → Plateforme prête pour le mémoire de master ✅
Cases non cochées → Reporter à l'agent pour correction ciblée