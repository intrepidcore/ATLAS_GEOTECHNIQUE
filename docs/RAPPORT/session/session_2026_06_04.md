---
name: session-2026-06-04
description: Décisions et corrections majeures — session UI/API/VfS/BLUP/DB
metadata: 
  node_type: memory
  type: project
  date: 2026-06-04
  originSessionId: f20eb3c3-5f46-4eec-b6fa-2d10bdc13c58
---

## Corrections critiques appliquées

### DB — Schéma sondages
- `public.sondages` = VIEW sans permissions pour user `atlas`
- Toutes les requêtes Rust dans `sondages.rs` doivent utiliser `atlas.sondages` (schéma explicite)
- `geocode_suggestions` n'existe pas — JOIN supprimé partout
- `geocoded_score` lu depuis `s.meta->>'geocoded_score'` uniquement (plus de JOIN)

### DB Manager
- `ENABLE_DB_MANAGER=true` ajouté dans `.env`
- `DATABASE_URL_ADMIN=postgresql://atlas:atlas@127.0.0.1:5433/atlas_clean` déjà présent
- Bug `[object Object]` : `api.ts` ligne 89 — `rawMsg` doit être converti en string avant affectation à `error.message`
- `App.tsx` rendu défensif : `typeof error === 'string' ? error : JSON.stringify(error)`

### BLUP Fusion — moteur Rust (nouveau)
- Fichier : `services/api-geo/src/blup_fusion.rs`
- Routes : `POST /ai/fusion/run`, `GET /ai/fusion/status`
- Utilise `sqlx::query()` NON-macro (pas `sqlx::query!()`) car `SQLX_OFFLINE=true` en Docker
- Rayon parallelisme sur 29 407 mailles
- Rétrocompatibilité Python : `ked_rk_fusion.py --use-rust-api http://... --rust-jwt TOKEN`

### MTGP — optimiseur Adam GPU
- `mtgp_geotechnique.py` : `gpflow.optimizers.Scipy()` → `tf.optimizers.Adam(lr=0.01)` + `@tf.function`
- `tf.config.experimental.set_memory_growth` activé avant toute op TF
- Convergence : plateau ELBO < 1e-4 pendant 300 iter → arrêt anticipé

### Panneau thématique UI
- Catégories : 4 familles uniquement (`couverture`, `argilosite`, `portance`, `insitu`) — `ia_ag` et `personnalise` retirés du dropdown
- Labels corrigés : "Couverture terrain", "In-situ & pressiométrique"
- `OBJECTIF_BASE_FILTER.portance = []` — ML ne modélise pas CBR/γd/wopt (passant_80um/passant_2mm retirés)
- `OBJECTIF_BASE_FILTER.insitu = []` — ML ne modélise pas Rd/NSPT/Em/Pl
- ML L3 VfS : désactivé seulement si `status === 'error'` — `not_computed` = accessible avec badge ⚠
- Messages contextuels quand source ML incompatible avec catégorie
- Warning RK H2 : banner orange si source=l2a_rk + horizon=H2 + param VBS ou EG

### Suggestions stats endpoint
- `/suggestions/stats` : `public.geocode_suggestions` inexistant → remplacé par proxy sur `atlas.sondages`

---

## VfS — État et blocage documenté

**`atlas.maille_spectral_vfs`** : 29 407 lignes, `vbs_vfs_pred = NULL` partout.

**Raison :** Script `vfs_extract_spectral.py` corrigé (bug UUID CONV-06) mais non relancé.

**Blocage intentionnel :** dépend de `session/audit/AUDIT_GEOCODAGE_CRITIQUE_2026-06-04.md`.
- 185 sondages ont la coordonnée fallback `POINT(1.0, 8.6)` → maille_code incorrects
- Les 96 sondages de calibration PLS sont potentiellement contaminés
- Relancer avant l'audit = modèle PLS sur données corrompues

**Séquence obligatoire avant relance VfS :**
1. Appliquer l'audit géocodage (re-géocodage sondages `location_mode=NULL`)
2. Recalculer les maille_code corrects
3. Ré-extraire features GEE (`sondage_spectral_features`)
4. Re-calibrer PLS
5. Relancer `vfs_extract_spectral.py --all-mailles`
6. Synchroniser dans `ai_interpolation_values` (parameter_id = `vbs_vfs`)

Documenté dans : `docs/RECHERCHE/RESULTATS_VFS_BLOC_C.md` section 7.

---

## Architecture Docker

- `atlas-api-geo` image reconstruite 2026-06-04 15:30 UTC (avec fixes sondages + blup_fusion)
- `atlas-ui` image reconstruite 2026-06-04 (avec fixes thematic panel)
- Port 8000 = Docker api-geo (PAS un process natif Windows)
- Vite dev server port 1420 = UI en développement (HMR actif)

---

## Règles permanentes rappelées

| Règle | Valeur |
|-------|--------|
| DB port scripts | 5433 (PostgreSQL 17 natif) |
| DB port Docker API | 5432 (via `host.docker.internal:5433` dans le conteneur) |
| BLUP parameter_id DB | `vbs_fusion_h1` (pas `vbs_blup_h1`) |
| sqlx dans Docker | `query()` non-macro — SQLX_OFFLINE=true |
| Sondages table | `atlas.sondages` (pas `public.sondages`) |
| JWT dev | `admin@atlas.local / Atlas2024!` |
